# PRICE — ML×OR @ NeurIPS 2026 submission checklist

**Deadline: August 31, 2026 (AoE). Notification: September 29, 2026.**
Venue: Second Workshop on ML×OR, NeurIPS 2026, Atlanta (Dec 12 or 13).
Submit via OpenReview (NeurIPS 2026 Workshop MLxOR venue group).

## Verified in this build

- [x] 4-page main body in NeurIPS conference format (References begin p.5;
      verified with pdflatex + pdftotext — page 4 fills to the bottom margin)
- [x] Non-anonymous (`[final]` option; author block visible) — the workshop
      is single-blind
- [x] Unlimited references + appendix: full theorem register (App. A),
      complete proofs (App. B), verification table (App. C), extra figures
      (App. D), reproducibility statement (App. E)
- [x] All 7 figures generated by `posk-pipeline/experiments/figures.py`,
      deterministic from seeds
- [x] 0 unresolved citations / references; 0 significant overfull boxes
- [x] Every numeric claim in the paper traces to a row in
      `posk-pipeline/results/RESULTS.md` (36 rows, 0 FAIL), `OPEN1.md`,
      or `REALDATA.md`

## Before you submit (5 items)

1. **Fill in Vignesh's email + both affiliations** in the author block of
   `main.tex` (placeholders marked `[institution]`). Decide author order;
   the equal-contribution footnote is already set.
2. **Replace the repository placeholder URL** (`https://github.com/[repo]/price`,
   appears in the abstract) with the real public repo — push the
   `posk-pipeline/` + `mlxor-derivations/` folders from this zip as that repo
   first, since single-blind reviewers will click it.
3. **Style file**: this build uses the genuine `neurips_2025.sty` (ML×OR asks
   for "NeurIPS conference format"). When the official `neurips_2026.sty`
   drops, swap the file and change one line in the preamble; layout is
   historically identical, but re-verify the 4-page boundary after swapping.
4. **Journal pathway indication**: you may indicate at most one of
   Stochastic Systems / Mathematics of OR / Operations Research.
   Recommendation from the venue analysis: **Stochastic Systems** (APS's own
   journal; highest-probability fit for stability-of-stochastic-feedback +
   applied probability). MOR is the ambition alternative.
5. **Compile check on Overleaf** (or locally): `pdflatex → bibtex →
   pdflatex ×2`. Confirm References still start on page 5.

## Page-budget trim order (if the 2026 sty costs lines)

1. Fig. 2 width 0.41 → 0.38\textwidth
2. Fig. 1 minipanels 0.34 → 0.32\textwidth
3. The §5 "Headline rows" sentence (numbers live in App. C)
4. The Rothschild/Easley/Aghion citation clause in §3 (keep Lai–Robbins)

Never trim: the anchoring falsification, the structure-proofness boundary
(known-c breaks the floor), the UCB lower-raw-regret admission, or the
real-data provenance sentence — these are the paper's credibility.
