--- a/econml/paper/main.tex
+++ b/econml/paper/main.tex
@@ -68,7 +68,7 @@
 
 The test we know how to run is a single-agent test. A model is evaluated, stress-tested and certified alone, against a distribution treated as given. When the deployed model reshapes that distribution, the performative prediction literature supplies the correction: repeated retraining converges if and only if a modulus $m = \varepsilon\beta/\gamma$ stays below one, where $\varepsilon$ measures how strongly the environment responds and $\gamma$ is the learner's own objective curvature \citep{perdomo2020}. Certify $m<1$ and the loop is safe.
 
-It is not, once the agent has company. Consider dealers whose quotes reshape a common pool of informed flow, recommenders competing for one attention pool, or lenders pricing against one population of borrowers. Each agent's retraining changes the data every \emph{other} agent will learn from next, and no agent internalizes that effect. We call this a \textbf{learning externality}: every participant behaves rationally, passes its own test, and helps amplify a loop none of them chose. The intuition is not new. Banks that each diversify optimally hold similar portfolios and fail together \citep{beale2011,wagner2010}, and funds running similar strategies unwound together in August 2007 with the crowding invisible from any single risk report \citep{khandani2007}. What is new is that the agents now \emph{learn}, which changes both what determines the danger and what can be done about it.
+It is not, once the agent has company. Consider dealers whose quotes reshape a common pool of informed flow, recommenders competing for one attention pool, or lenders pricing against one population of borrowers. Each agent's retraining changes the data every \emph{other} agent will learn from next, and no agent internalizes that effect. We call this a \textbf{learning externality}: every participant behaves rationally, passes its own test, and helps amplify a loop none of them chose. The intuition is not new: banks that each diversify optimally fail together \citep{beale2011,wagner2010}, and funds running similar strategies unwound together in August 2007, the crowding invisible from any single risk report \citep{khandani2007}. What is new is that the agents now \emph{learn}, which changes both what determines the danger and what can be done about it.
 
 \paragraph{What determines it is not how many firms there are.}
 Let firm $i$ have a response Jacobian $E_i$ describing how its own deployment reshapes the flow it faces, and let $R$ be the correlation matrix of the vectorized $E_i$. We show the market's stability is governed by $\mN = \Neff\, m_1$ with $\Neff = 1 + \kappa(\lmax(R)-1)$, where $\kappa$ is the spillover between firms and $\lmax(R)$ is the \textbf{effective number of independent learners}. With $s$ the fraction of each response attributable to a common model, vendor or pretraining corpus, this reads $\mN = m_1(1 + \kappa s (N-1))$, so \textbf{the effective number of learners is the number of independent models, not the number of firms.} The quantity a competition regulator measures is the wrong one: fifty equal-share firms have a minimal Herfindahl index and look perfectly competitive, and if all fifty share one vendor they are dynamically a monoculture \citep{hirschman1964}.
@@ -82,10 +82,10 @@
 \section{Related work}
 
 \paragraph{Monoculture, and why it is static.}
-\citet{kleinberg2021} show that when many firms adopt the same algorithm welfare can fall even when the shared algorithm is the better one, because correlated selection destroys the option value of independent evaluation; \citet{bommasani2022} formalize systemic exclusion when many deployments inherit one model. \citet{peng2024} is the nearest neighbour and the first to carry market effects, a two-sided matching model in which many decision-makers share one evaluation algorithm; \citet{jagadeesan2023} make the supply-side move, characterizing what producers supply when they compete through a shared recommender. All four are equilibrium statements, read at the market's resting point. None expresses a stability boundary or a rate, and none carries an object for \emph{which direction} a firm perturbs the environment, so a market can be statically fine and dynamically fragile at the same shared-model fraction.
+\citet{kleinberg2021} show that when many firms adopt the same algorithm welfare can fall even when the shared algorithm is the better one, because correlated selection destroys the option value of independent evaluation; \citet{bommasani2022} formalize systemic exclusion when many deployments inherit one model. \citet{peng2024} is the nearest neighbour and the first to carry market effects, a two-sided matching model in which many decision-makers share one evaluation algorithm; \citet{jagadeesan2023} make the supply-side move, characterizing what producers supply when they compete through a shared recommender. All four are equilibrium statements read at the market's resting point: no stability boundary, no rate, and no object for \emph{which direction} a firm perturbs the environment, so a market can be statically fine and dynamically fragile at the same shared-model fraction. The premise, meanwhile, is measured: \citet{kim2025} find substantial error correlation across 350 deployed models, concentrated within shared providers.
 
 \paragraph{Multiplayer performative prediction, and the containment relation.}
-\citet{narang2023} is the nearest dynamics: multiple learners in a shared decision-dependent environment, with equilibrium concepts and convergence conditions for repeated retraining. Their condition bounds scalars, the Lipschitz sensitivity of the joint distribution map against objective curvature, uniformly over the joint action. Ours bounds a spectral radius in which those scalars appear only inside $m_1$. The relation is strict refinement rather than corollary, and Proposition~\ref{prop:containment} gives the witness: two markets agreeing on every scalar of that kind, differing in stability, and so indistinguishable to any criterion built from those scalars alone. The substitution $\mathbf{1}\mathbf{1}^{\top}\!\to R$ adds an object rather than tightening a constant, and that object carries monoculture, provenance, the supply chain and every policy statement below. The single-learner lazy-deployment slope of \citet{mendlerdunner2020} is what Theorem~\ref{thm:cadence} composes with multi-agent amplification, a composition not previously made; the corrected update of \citet{izzo2021} is what Theorem~\ref{thm:mixed} puts into the game, having only ever been run single-learner.
+\citet{narang2023} is the nearest dynamics: multiple learners in a shared decision-dependent environment, with equilibrium concepts and convergence conditions for repeated retraining. Their condition bounds scalars, the Lipschitz sensitivity of the joint distribution map against objective curvature, uniformly over the joint action. Ours bounds a spectral radius in which those scalars appear only inside $m_1$. The relation is strict refinement rather than corollary, and Proposition~\ref{prop:containment} gives the witness: two markets agreeing on every scalar of that kind, differing in stability, and so indistinguishable to any criterion built from those scalars alone. The substitution $\mathbf{1}\mathbf{1}^{\top}\!\to R$ adds an object rather than tightening a constant, and that object carries monoculture, provenance, the supply chain and every policy statement below. Two adjacent loops fix the corner this paper opens: \citet{piliouras2023} drive many predictors of one common outcome into chaos, the monoculture corner $R=\mathbf{1}\mathbf{1}^{\top}$ taken past its linearization, and the consensus agents of \citet{li2022} couple through a constraint they choose rather than an environment they share, so no externality arises. The single-learner lazy-deployment slope of \citet{mendlerdunner2020} is what Theorem~\ref{thm:cadence} composes with multi-agent amplification, a composition not previously made; the corrected update of \citet{izzo2021} is what Theorem~\ref{thm:mixed} puts into the game, having only ever been run single-learner.
 
 \paragraph{Systemic risk, epidemiology, and the statistic.}
 \citet{beale2011} asked this question of banks, showing individually prudent portfolio choices can be collectively destabilizing; their agents do not learn, so the homogeneity is a portfolio overlap rather than an alignment of feedback directions, and there is no second instrument to trade diversity against. \citet{diekmann1990} converts the epidemiological parallel from analogy into identity: $R_0$ in a heterogeneous population is \emph{defined} as the spectral radius of a next-generation operator, and $\mN$ is the spectral radius of a linearized joint retraining operator, so the two thresholds are the same statement and the vaccination law transfers with its refinements. The largest eigenvalue of a correlation matrix as an effective count is meanwhile standard in random-matrix finance \citep{laloux1999,plerou2002}, signal processing \citep{roy2007} and ecology \citep{hill1973}; the novelty is the matrix it is computed from, response Jacobians rather than returns, and the condition it enters.
@@ -94,10 +94,10 @@
 \label{sec:setup}
 
 \paragraph{The base result.}
-Consider $N$ symmetric dealers quoting the same instruments and sharing one pool of informed order flow, coupled by a spillover $\kappa \in [0,1]$: one dealer tightening its quotes raises the toxic flow routed to every dealer, in proportion $\kappa$. Each retrains periodically on the flow it has observed, and in isolation a firm is stable exactly when its performative modulus satisfies $m_1 < 1$. The joint retraining map has Jacobian $J = -m_1[(1-\kappa)I + \kappa \mathbf{1}\mathbf{1}^{\top}]$, whose dominant common mode sits at $-m_1 \Neff$ with $\Neff = 1+\kappa(N-1)$, so the market crosses into instability a factor $\Neff$ earlier than any individual dealer's own loop would. Measured in a shared-pool simulator with real spreads, inventory and liquidity feedback, amplification is $1.74\times$ at $N=2$ and $3.16\times$ at $N=3$ against a linear prediction of $2$ and $3$ \citep{reflex2026}. This is inherited scaffolding; everything below replaces one object in it.
+Consider $N$ symmetric dealers quoting the same instruments and sharing one pool of informed order flow, coupled by a spillover $\kappa \in [0,1]$: one dealer tightening its quotes raises the toxic flow routed to every dealer, in proportion $\kappa$. Each retrains periodically on the flow it has observed, and in isolation a firm is stable exactly when its performative modulus satisfies $m_1 < 1$. The joint retraining map has Jacobian $J = -m_1[(1-\kappa)I + \kappa \mathbf{1}\mathbf{1}^{\top}]$, whose dominant common mode sits at $-m_1 \Neff$ with $\Neff = 1+\kappa(N-1)$, so the market crosses into instability at a response strength a factor $\Neff$ smaller than any individual dealer's own loop tolerates. Measured in a shared-pool simulator with real spreads, inventory and liquidity feedback, amplification is $1.74\times$ at $N=2$ and $3.16\times$ at $N=3$ against a linear prediction of $2$ and $3$ \citep{reflex2026}. This is inherited scaffolding; everything below replaces one object in it.
 
 \paragraph{Two questions, and they are not the same question.}
-Is each agent stable alone, $m_1 < 1$? And is the collection stable when its members interact, $\mN < 1$? A shared environment separates them, because the loop closes through that environment and nothing in dealer A's validation set contains dealer B. \textbf{Agent count is not a descriptive characteristic of a market; it enters the stability condition.} Read as economics, this is a \emph{technological} rather than pecuniary externality \citep{buchanan1962}: a price movement is a transfer, but reshaping the distribution a competitor learns from changes everyone's forecasting technology, which is a real efficiency loss. We call $\mN$ the \textbf{feedback reproduction number}: the factor by which a common perturbation is amplified per retraining round.
+Is each agent stable alone, $m_1 < 1$? And is the collection stable when its members interact, $\mN < 1$? A shared environment separates them, because the loop closes through that environment and nothing in dealer A's validation set contains dealer B. \textbf{Agent count is not a descriptive characteristic of a market; it enters the stability condition.} Read as economics, this is a \emph{technological} rather than pecuniary externality \citep{buchanan1962}: reshaping the distribution a competitor learns from degrades everyone's forecasting technology rather than transferring surplus. We call $\mN$ the \textbf{feedback reproduction number}: the factor by which a common perturbation is amplified per retraining round.
 
 \paragraph{Objects and standing assumptions.}
 Firm $i$ has a response Jacobian $E_i \in \mathbb{R}^{d\times d}$. The \emph{alignment matrix} $R = (r_{ij})$ collects $r_{ij} = \langle \mathrm{vec}\,E_i, \mathrm{vec}\,E_j\rangle / (\|E_i\|_F \|E_j\|_F)$, a correlation matrix of feedback directions rather than of returns; it is positive semidefinite with unit diagonal.
@@ -147,7 +147,7 @@
 Fix $N \geq 2$, $\kappa \in (0,1]$ and $m_1$ with $1/(1+\kappa(N-1)) < m_1 < 1$. Compare the market at $R = I$ with the market at $R = \mathbf{1}\mathbf{1}^{\top}$. The two agree on every scalar a uniform Lipschitz-and-curvature criterion can read: the same response magnitude $\varepsilon$ by \textup{(H2)}, the same objective curvature $\gamma$ and sensitivity $\beta$ by \textup{(H4)}, hence the same $m_1$ and the same spillover $\kappa$. Their radii are $\mN = m_1$ and $\mN = m_1(1+\kappa(N-1))$, which the choice of $m_1$ places on opposite sides of one. Any criterion measurable from those scalars alone therefore returns the same verdict for both, and is wrong about one of them.
 \end{proposition}
 
-At $N=20$, $\kappa=0.8$ and $m_1 = 0.15$ the orthogonal market runs $\mN = 0.15$ and the monoculture $\mN = 2.43$: one converges, the other diverges, and no Lipschitz constant in either model distinguishes them. This is why Theorem~\ref{thm:alignment} is a strict refinement of a uniform condition rather than a corollary of one. The object doing the separating is $R$, which no scalar criterion carries.
+At $N=20$, $\kappa=0.8$ and $m_1 = 0.15$ the orthogonal market runs $\mN = 0.15$ and the monoculture $\mN = 2.43$: one converges, the other diverges, and no Lipschitz constant in either model distinguishes them. The object doing the separating is $R$, which no scalar criterion carries.
 
 \paragraph{The supply chain.}
 Decompose $E_i = \sqrt{s}\,E_{\text{shared}} + \sqrt{1-s}\,\Xi_i$, where $s\in[0,1]$ is the fraction attributable to a shared foundation model, vendor or pretraining corpus. Alignments concentrate at $r_{ij}\to s$ with $O(1/d)$ fluctuation and an explicit failure probability, so $\Neff \to 1 + \kappa s (N-1)$ and every result below inherits $s$ for free: fifty dealers fine-tuning one vendor's model are dynamically ${\sim}1+49\kappa$ learners at $s\approx 1$ and barely more than one at $s \approx 0$.
@@ -158,7 +158,7 @@
 \section{Result 2: the crowding-cadence frontier}
 \label{sec:cadence}
 
-The first lever is quantity regulation, and it is the one a firm can pull alone: move less per round. A learner taking only $K$ gradient steps per deployment realizes outer-map slope $\mu(K) = -m + c^K(1+m)$, where $c = 1-\eta\gamma$ is the inner per-step contraction \citep{mendlerdunner2020}. Because $c$ is built from the learner's own objective curvature and step size, it does not depend on how many competitors it has, and that independence is what makes the composition below work. We verify it by measuring the realized contraction inside the joint market.
+The first lever is quantity regulation, and it is the one a firm can pull alone: move less per round. A learner taking only $K$ gradient steps per deployment realizes outer-map slope $\mu(K) = -m + c^K(1+m)$, where $c = 1-\eta\gamma$ is the inner per-step contraction \citep{mendlerdunner2020}. Because $c$ is built from the learner's own objective curvature and step size, it does not depend on how many competitors it has, and that independence, verified by measuring the realized contraction inside the joint market, is what makes the composition below work.
 
 \begin{theorem}[Systemic cadence window]
 \label{thm:cadence}
@@ -172,14 +172,11 @@
 The slope on the mode carrying $\lambda_i(R)$ is strictly decreasing in $\lambda_i$, which puts every slope at or below $c^K<1$, so the upper side of the constraint never binds at any cadence, and places the binding mode at $\lmax$ whatever the sign pattern of $R$.
 
 \paragraph{Critical crowding.}
-The same inequality reads $\mN < (1+c^K)/(1-c^K)$, and since $K$ is a positive integer the most permissive case is $K=1$. A stabilizing cadence exists at all if and only if $\mN < (1+c)/(1-c)$, a factor of $9$ at $c=0.8$. Past that the market diverges at every cadence, however small the step budget. Moving less per round is the only unilateral defence, and past the critical level it runs out.
+The same inequality reads $\mN < (1+c^K)/(1-c^K)$, and since $K$ is a positive integer the most permissive case is $K=1$. A stabilizing cadence exists at all if and only if $\mN < (1+c)/(1-c)$, a factor of $9$ at $c=0.8$. Past that the market diverges at every cadence: the one unilateral defence runs out. Nor is it free: the $c^K$ that widens the stability margin is exactly the deployed model's staleness, its residual gap to its own best response (Appendix~\ref{app:cadence}).
 
 \paragraph{What the supply chain does to the window.}
 Substituting Result~1 makes $K_{\max}$ a function of vendor concentration at fixed firm count: at $m_1 = 0.15$, $\kappa = 0.8$, $N=30$ and $c=0.8$, the window runs $20.68$ steps at $s = 0.25$, $5.28$ at $s = 0.5$ and $2.53$ at $s = 1$. Raising vendor concentration cuts every incumbent's step budget eightfold while its competitors do nothing at all. The externality in its sharpest form is not that \textbf{a competitor's entry consumes your step budget}, but that \textbf{a competitor's choice of vendor does too}, without their entering.
 
-\paragraph{What the lever costs.}
-Cadence buys stability with staleness, and the exchange rate is the same quantity on both sides: after a retraining round the deployed model's gap to its own best response is exactly $c^K$ times what it was, so the $c^K$ that widens the stability margin is the staleness.
-
 \begin{figure}[t]
 \centering
 \begin{subfigure}{0.40\textwidth}
@@ -196,7 +193,7 @@
 \section{Result 3: herd immunity, and diversity as its substitute}
 \label{sec:herd}
 
-The second lever is a technology mandate, and unlike cadence it is not something a firm can pull alone to any effect. A \emph{corrected} firm runs the feedback-aware update \citep{izzo2021,miller2021}, so its retraining is governed by the objective curvature $\gamma_{\mathrm{PO}}$ in place of the cobweb and its modulus is scaled by $\theta = \gamma/\gamma_{\mathrm{PO}} < 1$. What is new is running it inside the game: a mixed market of $N_b$ blind firms and $N - N_b$ corrected ones, corrected fraction $\rho = 1 - N_b/N$. Correction changes a firm's gain and not its response direction, so $R$ is untouched.
+The second lever is a technology mandate, and unlike cadence it is not something a firm can pull alone to any effect. A \emph{corrected} firm runs the feedback-aware update \citep{izzo2021,miller2021}, so its retraining is governed by the objective curvature $\gamma_{\mathrm{PO}}$ in place of the cobweb and its modulus is scaled by $\theta = \gamma/\gamma_{\mathrm{PO}} < 1$. What is new is running it inside the game: a mixed market of $N_b$ blind firms and $N - N_b$ corrected ones, corrected fraction $\rho_c = 1 - N_b/N$. Correction changes a firm's gain and not its response direction, so $R$ is untouched.
 
 \begin{theorem}[Mixed-market stability]
 \label{thm:mixed}
@@ -229,7 +226,7 @@
 At $\mN = 2.5$ the corrected update must deliver a $2.5\times$ curvature improvement or un-blinding cannot stabilize the market whatever fraction is treated. This parallels Section~\ref{sec:cadence}'s critical crowding: each lever has a regime past which it stops working, and knowing both is what makes trading them off honest rather than an extrapolation.
 
 \paragraph{The synthesis.}
-$\rho^{*}$ increases in $s$ wherever it is positive, so a market reaches stability along either axis. At $N=20$, $m_1 = 0.15$ and $\kappa = 0.8$ a monoculture needs $\rho^{*} = 0.596$, which is $12$ corrected firms; at $s = 0.5$ it needs five; at $s = 0.2$ it needs none at all. The regulator's count is $N - \lceil N_c \rceil + 1$ rather than $\lceil \rho^{*}N \rceil$, and the two differ by a firm exactly when $N_c$ is an integer.
+$\rho^{*}$ increases in $s$ wherever it is positive, so a market reaches stability along either axis. At $N=20$, $m_1 = 0.15$ and $\kappa = 0.8$ a monoculture needs $\rho^{*} = 0.596$, which is $12$ corrected firms; at $s = 0.5$ it needs five; at $s = 0.2$ it needs none at all. The count in whole firms, and the corner where the obvious rounding is off by one, are in Appendix~\ref{app:mixed}.
 
 \paragraph{The economic reading.}
 Correction is a public good: an un-blinded firm captures a private benefit while the stability it contributes accrues to everyone, so the market free-rides below the threshold and does not reach $\rho^{*}$ unaided \citep{bergstrom1986}. Model diversity has the same structure, a firm choosing an idiosyncratic vendor paying privately and supplying systemic stability for free. \textbf{The substitution is therefore between two goods that are both under-supplied}, not between a good and a bad.
@@ -243,7 +240,7 @@
 \begin{subfigure}{0.40\textwidth}
 \includegraphics[width=\linewidth]{figures/fig_substitution.pdf}
 \end{subfigure}
-\caption{Left: stability against corrected fraction at $N=20$, $m_1=0.15$, $\kappa=0.8$, $s=1$, across efficacies; the gap between the exact threshold and the perfect-correction limit is a result, not an error bar. Right: \textbf{the substitution frontier}, the $(\rho,s)$ iso-stability curve, matching at 18 of 18 points. Both are dry runs (Section~\ref{sec:experiments}).}
+\caption{Left: stability against corrected fraction at $N=20$, $m_1=0.15$, $\kappa=0.8$, $s=1$, across efficacies; the gap between the exact threshold and the perfect-correction limit is a result, not an error bar. Right: \textbf{the substitution frontier}, the $(\rho_c,s)$ iso-stability curve, matching at 18 of 18 points. Both are dry runs (Section~\ref{sec:experiments}).}
 \label{fig:herd}
 \end{figure}
 
@@ -256,7 +253,7 @@
 
 \begin{lemma}[Marginal crowding share]
 \label{lem:share}
-At the symmetric point, $\partial \mN/\partial m_i = \Neff v_i^2$ where $v$ is the leading eigenvector of the coupling matrix. The shares sum to $\Neff$ rather than to one, and on exchangeable $R$ each equals $\Neff/N$.
+At the symmetric point, with $\kappa>0$ and $\lmax(R)$ simple, $\partial \mN/\partial m_i = \Neff v_i^2$ where $v$ is the leading eigenvector of the coupling matrix. The shares sum to $\Neff$ rather than to one, and on exchangeable $R$ each equals $\Neff/N$.
 \end{lemma}
 
 \paragraph{Where a sign condition is needed, and where it is not.}
@@ -273,7 +270,7 @@
 A per-unit fee $t^{*}$ on aggressiveness makes the private condition coincide with the social one, so the taxed decentralized equilibrium implements the symmetric social optimum.
 \end{theorem}
 
-\textbf{Theorem~\ref{thm:wedge} characterizes the exchangeable symmetric point}, which covers the monoculture, orthogonal and supply-chain configurations but switches off the alignment heterogeneity Result~1 exists to represent. Away from it a firm sitting on the crowded mode owes more than its headcount share, and the asymmetric wedge is open (Appendix~\ref{app:deferred}). With symmetric weights the private condition ignores the fraction $(N-1)/N$ of the marginal variance cost borne by other firms, plus the entire client-side exposure. $t^{*}$ is strictly increasing in $N$, $\kappa$, $s$ and $\mN$, and diverges at the boundary like $(1-\mN)^{-2}$. The rate is the economically loaded part: a regulator setting a fee from a comfortable-looking market and holding it fixed is setting it far too low by the time the market is close to instability.
+\textbf{Theorem~\ref{thm:wedge} characterizes the exchangeable symmetric point}, which covers the monoculture and supply-chain configurations at any $s>0$, but excludes the orthogonal corner, where the leading eigenvalue is degenerate and firms decouple, and switches off the alignment heterogeneity Result~1 exists to represent. Away from it a firm sitting on the crowded mode owes more than its headcount share, and the asymmetric wedge is open (Appendix~\ref{app:deferred}). With symmetric weights the private condition ignores the fraction $(N-1)/N$ of the marginal variance cost borne by other firms, plus the entire client-side exposure. $t^{*}$ is strictly increasing in $N$, $\kappa$, $s$ and $\mN$, and diverges at the boundary like $(1-\mN)^{-2}$. The rate is the economically loaded part: a regulator setting a fee from a comfortable-looking market and holding it fixed is setting it far too low by the time the market is close to instability.
 
 \paragraph{The provenance channel.}
 $\mN$ depends on $s$, so the wedge has a second channel: $\partial \mN/\partial s = m_1\kappa(N-1)$ against $\partial\mN/\partial a_i = (\Neff/N)\mu'(a_i)$. The same wedge therefore prices \textbf{shared-model adoption} directly, which turns the monoculture externality into a fee rather than a warning. The two channels are not symmetric. A firm's footprint through its own aggressiveness is bounded, since $\Neff/N$ falls to $\kappa$ from above, while the provenance channel is \textbf{linear in $N$ and does not decay} against a private gain that does not grow with market size. Unregulated adoption therefore runs toward the unstable configuration, which is why the diversity floor of Section~\ref{sec:herd} is a different instrument from the cadence cap of Section~\ref{sec:cadence} rather than a restatement of it (Appendix~\ref{app:wedge}).
@@ -290,7 +287,7 @@
 \section{Experiments}
 \label{sec:experiments}
 
-Six panels, one per result, each with its evidential status in its own row of Table~\ref{tab:panels}, because the six are not the same kind of evidence. A panel marked \textbf{measured} ran in the order-flow simulator, a genuine $N$-dealer market with informed flow, spread and inventory that reduces bit for bit to the single-dealer market at $N=1$. A panel marked \textbf{dry run} ran in a linearized reference environment realizing the model's response geometry and nothing else: it checks that the closed forms were derived from the map we believe they were derived from, which is a check on the derivation rather than evidence about dynamics, and it has caught real errors. It establishes nothing about a market. Every closed form carries a numerical certificate and the harness exits nonzero on any disagreement. Appendix~\ref{app:experiments} gives the protocol, which is deterministic from a (config, seed) pair and estimates stability from realized trajectories rather than from an eigensolve. The simulator is described generically pending an artifact release, with code offered to reviewers as anonymized supplementary material.
+Six panels, each with its evidential status in its own row of Table~\ref{tab:panels}, because they are not the same kind of evidence. A panel marked \textbf{measured} ran in the order-flow simulator, a genuine $N$-dealer market with informed flow, spread and inventory that reduces bit for bit to the single-dealer market at $N=1$. A panel marked \textbf{dry run} ran in a linearized reference environment realizing the model's response geometry and nothing else: it checks that the closed forms were derived from the map we believe they were derived from, which is a check on the derivation rather than evidence about dynamics, and it has caught real errors. It establishes nothing about a market. Every closed form carries a numerical certificate and the harness exits nonzero on any disagreement. Appendix~\ref{app:experiments} gives the protocol, which is deterministic from a (config, seed) pair and estimates stability from realized trajectories rather than from an eigensolve. The simulator is described generically pending an artifact release, with code offered to reviewers as anonymized supplementary material.
 % CAMERA-READY ONLY. Uncomment the line below once the paper is de-anonymized.
 % Do NOT enable for the double-blind submission: the URL names an author and
 % breaks the blind.
@@ -338,7 +335,7 @@
 Every quantity above is hidden from the regulator these results address, and near the boundary the system tells on itself. As $\mN$ approaches one, the leading principal component of public quotes has lag-1 autocorrelation approaching one in magnitude, negative because the common mode oscillates, and variance share growing like $1/(1-\mN)$. A supervisor can therefore estimate distance to instability from public prices alone, and the estimate sharpens as the market nears the boundary it watches for. Consistency, sample complexity and a real-data panel are deferred, the last scoped in the appendix as consistency evidence with a placebo rather than identification.
 
 \paragraph{Limitations.}
-The analysis is linearized around the joint equilibrium (A1), and panel 1 shows what that costs in a real market. \textbf{The heterogeneous sweep is closed-form agreement, not measurement}, which is the largest gap in the work; routing liquidity and impact through the response profiles is the identified repair. Theorem~\ref{thm:alignment} is exact for equal moduli (A2) and a two-sided bound otherwise. Share-weighted alignment, the block-secular reduction when (H1) fails, and the sharp concentration rate are named extensions. The $1-1/R_0$ collapse assumes the $\kappa=s=1$ corner, three or more correction levels return a genuine secular equation, and correction that moves a response direction rather than a gain is not modelled. Deployment is synchronous (A3), and heterogeneous clocks break the eigenvector sharing Theorem~\ref{thm:cadence} uses. Pricing instability through stationary variance is itself a modeling choice.
+The analysis is linearized around the joint equilibrium (A1), and panel 1 shows what that costs in a real market. \textbf{The heterogeneous sweep is closed-form agreement, not measurement}, which is the largest gap in the work; routing liquidity and impact through the response profiles is the identified repair. Theorem~\ref{thm:alignment} is exact for equal moduli (A2) and a two-sided bound otherwise; share-weighted alignment, the block-secular reduction when (H1) fails, and the sharp concentration rate are named extensions. The $1-1/R_0$ collapse assumes the $\kappa=s=1$ corner, three or more correction levels return a genuine secular equation, and correction that moves a response direction rather than a gain is not modelled. Deployment is synchronous (A3), and heterogeneous clocks break the eigenvector sharing Theorem~\ref{thm:cadence} uses. Pricing instability through stationary variance is itself a modelling choice.
 
 \paragraph{Conclusion.}
 \textbf{Individual model evaluation cannot certify a market of models.} The quantity separating the two questions is the effective number of independent learners, which no evaluation of a single model can see: a firm can pass every test available to it and still be one of the reasons its market fails. Regulation of shared general-purpose models sets thresholds on model scale and ties nothing to a measurable systemic quantity. This paper supplies one, with two substitutable instruments and a price.
@@ -368,10 +365,14 @@
 
 \bibitem[Khandani and Lo(2007)]{khandani2007} A.~E. Khandani and A.~W. Lo. What happened to the quants in August 2007? \emph{Journal of Investment Management}, 5(4):29--78, 2007.
 
+\bibitem[Kim et al.(2025)]{kim2025} E.~M. Kim, A.~Garg, K.~Peng, and N.~Garg. Correlated errors in large language models. In \emph{ICML}, 2025.
+
 \bibitem[Kleinberg and Raghavan(2021)]{kleinberg2021} J.~Kleinberg and M.~Raghavan. Algorithmic monoculture and social welfare. \emph{PNAS}, 118(22):e2018340118, 2021.
 
 \bibitem[Laloux et al.(1999)]{laloux1999} L.~Laloux, P.~Cizeau, J.-P. Bouchaud, and M.~Potters. Noise dressing of financial correlation matrices. \emph{Physical Review Letters}, 83(7):1467--1470, 1999.
 
+\bibitem[Li et al.(2022)]{li2022} Q.~Li, C.-Y. Yau, and H.-T. Wai. Multi-agent performative prediction with greedy deployment and consensus seeking agents. In \emph{NeurIPS}, 2022.
+
 \bibitem[Mendler-D\"unner et al.(2020)]{mendlerdunner2020} C.~Mendler-D\"unner, J.~C. Perdomo, T.~Zrnic, and M.~Hardt. Stochastic optimization for performative prediction. In \emph{NeurIPS}, 2020.
 
 \bibitem[Miller et al.(2021)]{miller2021} J.~P. Miller, J.~C. Perdomo, and T.~Zrnic. Outside the echo chamber: Optimizing the performative risk. In \emph{ICML}, 2021.
@@ -384,6 +385,8 @@
 
 \bibitem[Perdomo et al.(2020)]{perdomo2020} J.~C. Perdomo, T.~Zrnic, C.~Mendler-D\"unner, and M.~Hardt. Performative prediction. In \emph{ICML}, 2020.
 
+\bibitem[Piliouras and Yu(2023)]{piliouras2023} G.~Piliouras and F.-Y. Yu. Multi-agent performative prediction: From global stability and optimality to chaos. In \emph{EC}, 2023.
+
 \bibitem[Plerou et al.(2002)]{plerou2002} V.~Plerou, P.~Gopikrishnan, B.~Rosenow, L.~A.~N. Amaral, T.~Guhr, and H.~E. Stanley. Random matrix approach to cross correlations in financial data. \emph{Physical Review E}, 65(6):066126, 2002.
 
 \bibitem[Roy and Vetterli(2007)]{roy2007} O.~Roy and M.~Vetterli. The effective rank: A measure of effective dimensionality. In \emph{EUSIPCO}, 2007.
--- a/econml/paper/appendix.tex
+++ b/econml/paper/appendix.tex
@@ -26,7 +26,7 @@
 $K$ & gradient steps per deployment & integer $\geq 1$ \\
 $\eta$ & inner learning rate & $>0$ \\
 $c$ & inner per-step contraction, $1-\eta\gamma$ & $(0,1)$ \\
-$\gamma_{PO}$ & curvature governing the corrected dynamics & $>\gamma$ \\
+$\gamma_{PO}$ & curvature governing the corrected dynamics & $\geq\gamma$ \\
 $\theta$ & $\gamma/\gamma_{PO}$, the correction factor \emph{(new)} & $(0,1]$ \\
 $e$ & correction efficacy, $1-\theta$ \emph{(new)} & $[0,1)$ \\
 $N_b$ & blind (uncorrected) firms \emph{(new)} & $0 \ldots N$ \\
@@ -352,7 +352,7 @@
 \]
 \end{proof}
 
-Two readings follow and they point opposite ways. The reassuring one is that correction never backfires: more correction is always weakly stabilizing, so there is no perverse configuration in which un-blinding a firm destabilizes the market. The uncomfortable one is that $\rho(\theta) \geq \rho(0)$, so the strong-correction limit \emph{under-states} the radius. The limit is optimistic rather than conservative, and it can call a market stable that is unstable at any finite correction strength. On uniform random draws with $\theta \in [0.01,1]$ the limit calls $11.8\%$ of configurations stable that are not. Three worked cases, all of which the limit reads as comfortably stable:
+Two readings follow and they point opposite ways. The reassuring one is that correction never backfires: more correction is always weakly stabilizing, so there is no perverse configuration in which un-blinding a firm destabilizes the market. The uncomfortable one is that $\rho(\theta) \geq \rho(0)$, so the strong-correction limit \emph{under-states} the radius. The limit is optimistic rather than conservative, and it can call a market stable that is unstable at any finite correction strength. On uniform random draws the limit calls $11.8\%$ of configurations stable that are not ($N$ on $\{2,\ldots,39\}$, $N_b$ on $\{1,\ldots,N-1\}$, $m_1$ on $[0.02,0.9]$, $\kappa$ and $s$ on $[0.05,1]$, $\theta$ on $[0.01,1]$; the fraction moves with the sampling ranges, the direction never does). Three worked cases, all of which the limit reads as comfortably stable:
 
 \begin{center}
 \small
@@ -430,12 +430,12 @@
 \]
 The coefficient is $-\mN$ and not $\mN$ because the retraining Jacobian is $J = -m_1[(1-\kappa)I+\kappa R]$ and the common mode inherits its sign. The stationary variance is blind to that sign, since the recursion enters squared, which is why the same expression serves both. The sign is not lost information: it reappears in the lag-1 autocorrelation, which is $-\mN$ and therefore negative, so a crowded market shows oscillatory rather than persistent common-mode error. That is the observable the supervision estimator reads, and it is why a positive-autocorrelation diagnostic would look for the wrong thing. Certificate C19 checks both facts by simulation rather than asserting them.
 
-$V$ is the paper's price of instability, and it is a modeling choice: stationary variance is the smooth proxy for divergence risk, finite everywhere strictly inside the stable region and divergent exactly at the boundary the rest of the paper is about. Nothing below depends on the functional form beyond $V$ being positive, increasing and convex in $\mN$ on $[0,1)$, all three of which the closed form satisfies.
+$V$ is the paper's price of instability, and it is a modelling choice: stationary variance is the smooth proxy for divergence risk, finite everywhere strictly inside the stable region and divergent exactly at the boundary the rest of the paper is about. Nothing below depends on the functional form beyond $V$ being positive, increasing and convex in $\mN$ on $[0,1)$, all three of which the closed form satisfies.
 
 \begin{description}
 \item[(W1) Aggressiveness.] Each firm chooses $a_i > 0$, meaning cadence, learning rate or responsiveness: anything that scales its own modulus. Write $m_i = \mu(a_i)$ with $\mu$ differentiable, strictly increasing and common across firms. Aggressiveness moves a firm's gain and not its response direction, so $R$ and $\kappa$ are held fixed.
 \item[(W2) Benefit.] Aggressiveness buys tracking benefit $B(a_i)$, strictly increasing and strictly concave. Only firm $i$'s own benefit depends on $a_i$, so the entire interaction runs through $\mN$.
-\item[(W3) Exchangeable alignment.] $R$ is invariant under a common permutation of firms. The supply-chain alignment is exchangeable, as are the monoculture and orthogonal corners. This buys the uniform leading eigenvector at the symmetric point.
+\item[(W3) Exchangeable alignment.] $R$ is invariant under a common permutation of firms. The supply-chain alignment is exchangeable, as are the monoculture and orthogonal corners. This buys the uniform leading eigenvector at the symmetric point. Simplicity of the leading eigenvalue is also required, and it holds at monoculture and supply-chain alignment with $\kappa s > 0$; it fails at $R = I$, where the maximum is degenerate, firms decouple, and the marginal share is a subgradient rather than a derivative, so the orthogonal corner is outside Theorem~\ref{thm:wedge}'s scope.
 \item[(W4) Client exposure.] The planner's variance weight satisfies $W > \sum_i w_i$, with all weights positive.
 \end{description}
 
--- a/econml/paper/make_figures.py
+++ b/econml/paper/make_figures.py
@@ -93,7 +93,7 @@
 ax.axhline(1.0, ls=":", color="0.35", lw=1)
 ax.axvline(p4["rho_star_limit"], ls="--", color="k", lw=1,
            label=r"$\rho^*$, perfect correction")
-ax.set_xlabel(r"corrected fraction $\rho$")
+ax.set_xlabel(r"corrected fraction $\rho_c$")
 ax.set_ylabel(r"measured $m_N$")
 ax.legend(loc="upper right", framealpha=0.9)
 fig.savefig(OUT / "fig_herd.pdf")
@@ -113,7 +113,7 @@
 ax.plot(xs, [p["measured_rho"] for p in p5["curve"]], "o", ms=4.5, mfc="none",
         color="tab:orange", label="measured threshold")
 ax.set_xlabel(r"shared-model fraction $s$")
-ax.set_ylabel(r"corrected fraction $\rho$")
+ax.set_ylabel(r"corrected fraction $\rho_c$")
 ax.set_ylim(-0.03, 1.0)
 ax.legend(loc="upper left", framealpha=0.9)
 fig.savefig(OUT / "fig_substitution.pdf")
